{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "openmmo://schemas/id.schema.json",
  "title": "OpenMMO ID",
  "description": "Canonical identifier format for OpenMMO entities. Format: {category}_{local_id} where category is [a-z][a-z0-9]{1,15} and local_id is [a-z][a-z0-9_]{0,46}. Note: This validates SYNTAX ONLY. Category registration and existence validation are T003b/T006 scope.",
  "type": "string",
  "pattern": "^[a-z][a-z0-9]{1,15}_[a-z][a-z0-9_]{0,46}$",
  "minLength": 4,
  "maxLength": 64,
  "examples": [
    "item_iron_sword",
    "item_iron_sword_001",
    "reward_daily_bonus",
    "currency_gold"
  ]
}
